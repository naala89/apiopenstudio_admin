{% for messagetype, message in messages %}
  {% if messagetype == 'info' %}
    {% set message_type = 'info-message' %}
  {% endif %}
  {% if messagetype == 'warning' %}
    {% set message_type = 'warning-message' %}
  {% endif %}
  {% if messagetype == 'error' %}
    {% set message_type = 'error-message' %}
  {% endif %}
  {% for text in message %}
    <div class="row apiopenstudio-alert">
      <div class="col s10 m12">
        <div class="card {{ message_type }}">
          <div class="row">
            <div class="col s12 m10">
              <div class="card-content">
                <p>{{ text | raw }}</p>
              </div>
            </div>
            <div class="col s2 m2">
              <i class="small material-icons close-apiopenstudio-alert">close</i>
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endfor %}  
{% endfor %}
