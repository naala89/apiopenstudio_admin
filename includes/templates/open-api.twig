{% extends "page-no-footer.twig" %}

{% set title="OpenApi Documentation" %}

{% block content %}
  <link rel="stylesheet" type="text/css" href="/css/swagger-ui.css" />
  <style>
    html {
      box-sizing: border-box;
      overflow: -moz-scrollbars-vertical;
      overflow-y: scroll;
    }
    *, *:before, *:after {
      box-sizing: inherit;
    }
    body {
      margin:0;
      background: #fafafa;
    }
  </style>
  <div class="row">
    <form action="/open-api" method="get" class="openapi-application-select">
      <div class="col s3">
        <div class="input-field">
          <select name="appid">
            <option value="" selected>None</option>
            {% for application in applications %}
              <option value="{{ application.appid }}" {% if application.appid == appid %}selected{% endif %}>{{ application.name }}</option>
            {% endfor %}
          </select>
          <label for="application">Select an application.</label>
        </div>
      </div>
      <div class="col ss">
        <button class="btn" type="submit">View docs<i class="material-icons right">send</i>
        </button>
      </div>
    </form>
  </div>
  <div class="row">
    <div id="swagger-ui"></div>
  </div>
  <script src="/js/swagger-ui-bundle.js"> </script>
  <script src="/js/swagger-ui-standalone-preset.js"> </script>
  <script>
    window.addEventListener('load', () => {
      const ui = SwaggerUIBundle({
        spec: {{ openapi_def | raw }},
        dom_id: '#swagger-ui'
      })

      window.ui = ui
    })
  </script>
{% endblock %}
